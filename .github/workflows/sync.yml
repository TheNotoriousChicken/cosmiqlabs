name: Background Sync
on:
  schedule:
    - cron: '*/10 * * * *' # Every 10 minutes
  workflow_dispatch: # Allows manual trigger

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Hit Vercel Sync Endpoint
        run: |
          curl -X POST https://cosmiqlabs.vercel.app/api/sync
